# Requires Python 3.11+
# Source of truth is pyproject.toml — this file mirrors it for convenience.
# Install all extras with: pip install -e ".[dev]"

# Core
anthropic
deepeval>=1.0.0
litellm
better-profanity
detoxify
sentence-transformers
torch
duckduckgo-search>=6.2.0
ddgs
rank-bm25>=0.2.2
pydantic

# Demo (optional extra: pip install -e ".[demo]")
fastapi
uvicorn

# Testing (optional extra: pip install -e ".[test]")
datasets>=2.0.0
pytest
scikit-learn
